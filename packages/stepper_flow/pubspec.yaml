name: stepper_flow
description: "Flutter widgets for structuring multi-step flows with step navigation, progress indicators, and navigation controls while leaving step content to the application."
version: 0.0.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.13.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  material_ui: ^1.0.0
  meta: ^1.18.0
  smooth_page_indicator: ^2.0.1
