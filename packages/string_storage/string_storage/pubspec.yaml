name: string_storage
description: A low-level interface for reading and writing string values, abstracting the underlying persistence mechanism.
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.13.0-103.1.beta
