name: mdns_platform_check
description: Checks whether the current platform exposes native APIs for mDNS service discovery.
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  dbus: ^0.7.12
  jni: ^1.0.0
  jnigen: ^0.16.0
  platform: ^3.1.6
  win32: ^6.3.0
