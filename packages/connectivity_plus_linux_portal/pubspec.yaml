name: connectivity_plus_linux_portal
description: A Linux implementation of connectivity_plus that uses xdg_desktop_portal
version: 0.0.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  connectivity_plus_platform_interface: ^2.1.0
  xdg_desktop_portal: ^0.1.14
