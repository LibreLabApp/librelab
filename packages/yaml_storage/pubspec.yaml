name: yaml_storage
description: Adds YAML support on top of string_storage
version: 0.0.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.13.0-167.1.beta

dependencies:
  string_storage: ^0.0.1
  yaml: ^3.1.3
  yaml_edit: ^2.2.4
