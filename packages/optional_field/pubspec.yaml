name: optional_field
description: Distinguishes between an omitted field and a field explicitly set to null or a value.
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  meta: ^1.18.0
