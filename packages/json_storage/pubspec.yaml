name: json_storage
description: Adds JSON support on top of string_storage
version: 0.0.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.13.0-167.1.beta

dependencies:
  string_storage: ^0.0.1
  json_safe: ^0.0.1
  logging: ^1.3.0
