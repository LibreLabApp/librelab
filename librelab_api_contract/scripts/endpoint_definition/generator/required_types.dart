part of 'generator.dart';

// Types that are required and are used in the generated code.
// The code generator does not generate these types
// and assumes the consumer provides them (via an import).

const _httpEndpointReference = Reference('HttpEndpoint');
const _webSocketEndpointReference = Reference('WebSocketEndpoint');
const _httpMethodReference = Reference('HttpMethod');
const _stringReference = Reference('String');
