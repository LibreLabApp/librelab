name: librelab_shared
description: Shared code between server and Flutter app
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  email_validator: ^3.0.0
  meta: ^1.17.0
  result:
    git:
      url: https://github.com/EchoEllet/dart-packages.git
      ref: 4244700e2f2a99cf777d1e94c8ad9942f9b9432a
      path: packages/result

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
